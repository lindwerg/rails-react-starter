# This file contains a list of dependencies that are not part of the long term plan for the
# 'packs/users' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::ApplicationPolicy":
    violations:
    - dependency
    - layer
    - privacy
    files:
    - packs/users/app/policies/user_policy.rb
  "::ApplicationPolicy::Scope":
    violations:
    - dependency
    - layer
    - privacy
    files:
    - packs/users/app/policies/user_policy.rb
  "::ApplicationRecord":
    violations:
    - dependency
    - layer
    - privacy
    files:
    - packs/users/app/models/user.rb
packs/posts:
  "::Post":
    violations:
    - dependency
    - privacy
    files:
    - packs/users/app/models/user.rb
